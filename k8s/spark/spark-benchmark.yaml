apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: benchmark-3w-spark
  namespace: spark
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/spark-benchmark:latest
  imagePullPolicy: Always
  mainApplicationFile: "local:///opt/spark/scripts/run_spark_benchmark.py"
  sparkVersion: "3.5.3"
  restartPolicy:
    type: Never
  driver:
    cores: 2
    memory: "4g"
    serviceAccount: spark      # tem a IAM role com acesso S3 via IRSA
  executor:
    cores: 2
    coreRequest: "700m"
    coreLimit: "1500m"
    instances: 4
    memory: "3g"
    serviceAccount: spark  # IRSA: mesma role do driver — executors também precisam de acesso S3
  sparkConf:
    "spark.jars.ivy": "/tmp/.ivy"
    "spark.sql.shuffle.partitions": "8"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "com.amazonaws.auth.DefaultAWSCredentialsProviderChain"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.kubernetes.driverEnv.DATASET_PATH": "s3a://streaming-3w/3w/dataset"
    "spark.kubernetes.driverEnv.OUTPUT_PATH": "s3a://streaming-3w/results/spark"
    "spark.kubernetes.executor.serviceAccountName": "spark"
    # nanosAsLong via SQLConf só alcança o driver (spark.conf.set).
    # O leitor vetorizado nos executors usa SQLConf.get que não herda a sessão do driver.
    # Solução: usar o leitor row-based, que lê nanosAsLong do HadoopConf.
    # spark.hadoop.X=Y injeta X=Y no HadoopConf de todos os nós (driver + executors).
    "spark.sql.parquet.enableVectorizedReader": "false"
    "spark.hadoop.spark.sql.parquet.nanosAsLong": "true"
